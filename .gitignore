# IDE
.idea
.vscode

# Environment files (CRITICAL - prevents committing secrets)
.env
.env.local
.env.production
.env.staging
.env.development

# Database files (CRITICAL - contains sensitive data)
*.db
*.sqlite
*.sqlite3
noti.db
noti.sqlite
database.db

# Data directory (contains databases and sensitive files)
data/

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# Coverage files
coverage.txt

# Log files
*.log
logs/

# Config files with secrets
config.local.yaml
config.production.yaml
*.token

# Temporary files
tmp/
temp/
.tmp